export default class Category {
    /**
     *
     * @param {string} id
     * @param {string} title
     * @param {string} color
     */
    constructor(id, title, color) {
        this.id = id
        this.title = title
        this.color = color
    }
}
